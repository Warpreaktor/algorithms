package ru.warpreaktor.practicum;

import ru.warpreaktor.util.Utils;

/**
 * Задача. Дан массив целых чисел arr и целое число X.
 * Определите, существует ли в массиве такой непрерывный подмассив, что сумма его элементов равна X.
 */
public class sumArray {

    public static int[] sumArray(int[] arr, int x) {
        int sum = 0;
        int[] result = new int[0];
        int i = 0;
//
//        while (i < arr.length) {
//            int j = i;
//            while (j < arr.length && (sum = arr[j] + arr[i]) < x ) {
//                ++j;
//            }
//            result +=result;
//            i = j;
//        }
        return result;
    }

}
